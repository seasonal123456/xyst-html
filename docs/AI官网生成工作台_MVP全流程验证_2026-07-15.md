# AI官网生成工作台 MVP 全流程验证记录 2026-07-15

## 验证结论

- 已完成一次从客户上传资料到 Netlify 轻部署发布的端到端验证。
- 入口域名：`https://xinyingst.com`
- 成功报告：`logs/e2e-site-flow-20260715035857.json`
- 测试任务：`cmrljw6kr0003y8mzdeho3tlq`
- 生产数据库已确认：`publishStatus=published`，`publishedUrl`、`netlifySiteName`、`netlifySiteId`、`netlifyDeployId` 均已落库。

## 本次成功产物

- 预览页：`https://ai-site-generated-prod.oss-cn-guangzhou.aliyuncs.com/ai-website-workbench/generated/codex-sites/cmrljw6kr0003y8mzdeho3tlq-1784087942970/index.html`
- 标准交付包：`https://ai-site-generated-prod.oss-cn-guangzhou.aliyuncs.com/ai-website-workbench/generated/delivery-packages/standard-delivery-cmrljw6kr0003y8mzdeho3tlq-1784088302210.zip`
- Netlify 临时站：`https://xyste2e035857-xyst5.netlify.app`

## 已修复问题

- 修复预览完成后自动发布导致的 Netlify `409` 冲突。
- 当前配置：`SITE_AUTO_PUBLISH_ON_PREVIEW=false`
- 轻部署发布保留为按钮触发；重复点击发布接口会返回已有发布结果，不再因为重复发布直接失败。

## 当前生产配置要点

- 官网生成模式：`SITE_GENERATION_MODE=worker_queue`
- 自检质量门：`SITE_QUALITY_GATE_MODE=off`
- 增强后台模块：`SITE_ENABLE_ENHANCED_BACKEND_MODULES=false`
- 本机 worker 指向：`https://xinyingst.com`
- worker 日志：`D:\codex002\ai-image-mvp-stage1\site-worker.log`

## 临时配置与已知瓶颈

- ECS 当前无法访问模型网关 `192.168.2.186:3000/v1`，也无法直接访问 `api.openai.com`。
- 为保障 MVP 演示链路，生产环境临时使用：
  - `STYLE_IMAGE_PROVIDER=mock`
  - `COPY_PROVIDER=mock`
  - `ENABLE_MOCK_FALLBACK=true`
- 正式官网生成仍由本机 worker 执行，因此完整官网预览和交付包生成链路可以继续演示。
- 长期建议二选一：
  - 把风格图、文案生成也迁入本机 worker，由本机统一访问模型。
  - 给 ECS 配置公网可访问且稳定的模型网关。

## 2026-07-15 鑫源AI公网接口切换

- 已将生产模型入口从内网地址切换为鑫源AI公网入口：`https://xinyuanai666.com/v1`。
- 已确认本机和 ECS 均可访问该公网入口。
- 文案模型已切换为：`gpt-5.5-high`。
- 生图模型已切换为：`gpt-image-2`。
- 生产配置已从 mock 切换为真实接口：
  - `STYLE_IMAGE_PROVIDER=openai`
  - `COPY_PROVIDER=openai`
  - `OPENAI_BASE_URL=https://xinyuanai666.com/v1`
  - `OPENAI_IMAGE_MODEL=gpt-image-2`
  - `OPENAI_TEXT_MODEL=gpt-5.5-high`
  - `ENABLE_MOCK_FALLBACK=false`
- 已完成远端 `npm run build`，并重启 PM2 `xinyingst-ai-site`，状态为 online。
- 冒烟验证结果：
  - `/models` 鉴权通过。
  - `gpt-5.5-high` 文案接口返回成功。
  - `gpt-image-2` 生图接口返回 `b64_json`，当前项目代码可以处理。
  - 生产路由真实调用 `site_style_image` 成功 1 次，模型为 `gpt-image-2`。
- 新发现的产品/工程问题：当前风格图生成接口同步等待 3 张图全部完成后才写入数据库；真实 `gpt-image-2` 单张复杂风格图约需数分钟，用户侧容易表现为“卡住”。后续应优先把风格图生成改为队列模式，或逐张落库并显示进度。

## Netlify 用量记录

- 本次发布管理员备注记录：消耗 `0 credits`，剩余 `1000/1000`，下次刷新时间为 `2026-08-13T00:00:00.000-07:00`。
- Netlify 用量统计可能存在延迟，演示后仍建议在 Billing 页面复核。
