# AI官网生成工作台 阿里云同步记录 2026-07-14

## 本次同步内容

- 已将本地当前代码打包并同步到阿里云 ECS。
- 新 release：`/opt/xinyingst/releases/20260714-153827-mvp-direct-contact`
- 当前指针：`/opt/xinyingst/current -> /opt/xinyingst/releases/20260714-153827-mvp-direct-contact`
- PM2 进程：`xinyingst-ai-site`
- 启动目录：`/opt/xinyingst/releases/20260714-153827-mvp-direct-contact`

## 已同步的关键产品规则

- MVP 阶段官网生成默认规避表单、预约、会员、登录、客户专区和客资后台。
- 官网转化默认使用联系人、电话、微信、二维码/微信卡片、地址和固定联系 CTA。
- 增强后台能力后置，只有明确开启 `SITE_ENABLE_ENHANCED_BACKEND_MODULES=true` 后才规划 Supabase/Netlify Functions 表单、预约、会员和客户后台。
- 自检质量门保持关闭：`SITE_QUALITY_GATE_MODE=off`。
- 官网生成保持 worker 队列模式：`SITE_GENERATION_MODE=worker_queue`。

## 远端验证

- `npm run prisma:generate` 通过。
- `npm run db:init` 通过，SQLite 表结构已检查。
- `npm run build` 通过。
- ECS 本机应用端口 `127.0.0.1:3000` 返回 200。
- Nginx HTTPS 反代在 ECS 本机访问 `https://xinyingst.com/` 返回 200。
- `https://8.138.148.34/` 携带 `Host: xinyingst.com` 返回 200。
- `/api/site-compliance` 返回成功，当前 ICP 号尚未填写。

## 当前外部访问状态

- 2026-07-15 已完成 ICP 备案同步，备案号：`粤ICP备2026096063号`。
- DNS：`xinyingst.com` 与 `www.xinyingst.com` 均解析到 `8.138.148.34`。
- 外部访问 `https://xinyingst.com/` 返回 200。
- 外部访问 `https://www.xinyingst.com/` 返回 200。
- 外部访问 `http://xinyingst.com/` 返回 301，并跳转到 `https://xinyingst.com/`。
- `/api/site-compliance` 已返回 ICP 备案号。

## 备案前演示补充

- 已将 Nginx 的 HTTP 默认入口临时代理到 Next.js 应用，备份文件：`/etc/nginx/sites-available/default.bak-20260714-icp-demo`。
- 备案前可用临时演示入口：`http://8.138.148.34/`
- 客户开始页：`http://8.138.148.34/site/start`
- 管理员入口：`http://8.138.148.34/admin/customers`
- 2026-07-15 备案通过后，生产环境公开基准 URL 已切回：`PUBLIC_SITE_BASE_URL=https://xinyingst.com`、`NEXT_PUBLIC_SITE_BASE_URL=https://xinyingst.com`。
- 本机 worker 已启动并指向 `https://xinyingst.com`，当前持续轮询云端队列。日志：`D:\codex002\ai-image-mvp-stage1\site-worker.log`。
- 管理员登录接口 `POST /api/admin/login` 已验证成功。

## 备案通过后待办

1. 如有公安备案号，再写入：`SITE_POLICE_RECORD_NUMBER` 和 `SITE_POLICE_RECORD_URL`。
2. 从外网定期复查：
   - `https://xinyingst.com/`
   - `https://www.xinyingst.com/`
   - `https://xinyingst.com/api/site-compliance`
3. 如不再需要 IP HTTP 演示入口，可恢复 `/etc/nginx/sites-available/default.bak-20260714-icp-demo` 或保留 IP 入口作为内部演示入口。
4. 开始完整 MVP 测试前，确认本机 worker 已启动并能访问 ECS、OSS 和模型接口。
