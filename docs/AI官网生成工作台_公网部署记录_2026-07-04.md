# AI官网生成工作台 公网部署记录 2026-07-04

## 已完成

- ECS：阿里云 Ubuntu 22.04.5，公网 IP `8.138.148.34`。
- 域名：`xinyingst.com`、`www.xinyingst.com` 已解析到 ECS。
- 应用目录：`/opt/xinyingst/current`，release 位于 `/opt/xinyingst/releases/20260704-004513`。
- 共享数据目录：`/opt/xinyingst/shared`。
- 数据库：SQLite，`DATABASE_URL=file:/opt/xinyingst/shared/prod.db`，Prisma 迁移已完成。
- 运行环境：Node.js 22.23.1、PM2 7.0.3、Nginx、Certbot、Codex CLI 0.142.5。
- Next.js 生产构建通过，PM2 进程名：`xinyingst-ai-site`。
- Nginx 已反代到 `127.0.0.1:3000`。
- 安全组已放行：
  - HTTP 80：`0.0.0.0/0`
  - HTTPS 443：`0.0.0.0/0`
- SSL：Let's Encrypt 证书已签发并部署，证书域名包含 `xinyingst.com` 与 `www.xinyingst.com`，到期日 `2026-10-01`，Certbot 自动续期已启用。
- 公网 HTTPS 验证通过：
  - `https://xinyingst.com/` 返回 200
  - `https://www.xinyingst.com/` 返回 200

## 当前注意事项

- `http://xinyingst.com/` 当前返回阿里云 `Beaver 403`，请求未进入 Nginx。HTTPS 可正常访问。后续如需 HTTP 自动跳 HTTPS，需要检查阿里云侧备案/拦截策略。
- 生产 `.env` 里的模型网关为 `OPENAI_BASE_URL=http://192.168.2.186:3000/v1`。这是内网地址，ECS 无法访问，测试结果为连接超时。因此当前公网网站页面可访问，但文案生成、生图、Codex 最终官网生成等真实模型能力在 ECS 上会失败。

## 下一步

1. 配置 ECS 可访问的公网模型网关，或改用官方/可公网访问的 OpenAI-compatible endpoint。
2. 验证 `OPENAI_TEXT_MODEL=gpt-5.5`、`OPENAI_IMAGE_MODEL=gpt-image-2`、`SITE_IMAGE_MODEL=gpt-image-2`、`CODEX_SITE_MODEL=gpt-5.5` 在服务器端可用。
3. 做一次公网完整链路试单：上传资料、生成风格图、生成文案、生成官网、生成交付包。
4. 若准备正式对外客户访问，补充登录/注册、任务队列、用量限制、隐私政策和服务条款。
