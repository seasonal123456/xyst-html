# AI 官网 gpt-5.6-sol 远程建站稳定版上线记录

## 结论

- 正式站最终 HTML 已改由 ECS 后台 Worker 调用 `gpt-5.6-sol` 流式生成。
- ECS 不再执行 `npm install`、`prisma generate`、`next build` 等构建动作，只负责校验、解压、切换 release 和重启运行进程。
- Public 全流程已通过：登录、创建客户、上传资料、并行生成 3 张风格图、生成文案、远程 HTML、OSS 预览、标准交付 ZIP、Netlify 轻量化上线。
- 模板与 mock fallback 均保持关闭。

## 版本与部署包

- 代码提交：`731d58a fix: retry truncated remote HTML generation`
- Git 标签：`AI官网-gpt56sol远程建站稳定版-20260831`
- GitHub 隔离分支：`release/gpt56sol-20260830`
- 本地完整包：`D:\codex002\backups\AI-site-gpt56sol-full-deploy-20260831\AI-site-gpt56sol-full-deploy-20260831-r2.tar.gz`
- 完整包 SHA256：`97E97B890419895D64839CEB358B0EDA848EEAFB10043AC18D53039D7BA99885`
- ECS release：`/opt/xinyingst/releases/20260831-gpt56sol-retry-r3`
- 当前软链接：`/opt/xinyingst/current -> /opt/xinyingst/releases/20260831-gpt56sol-retry-r3`
- 回滚记录：`/opt/xinyingst/shared/rollback-current-before-20260831-gpt56sol-retry-r3.txt`

完整包包含已构建的 `.next`、Linux 依赖、源码、教程视频、校验清单和只解压安装脚本。包中不含 `.env`、密钥、数据库、日志、私钥或用户上传数据。

## 稳定性修复

第一次 Public 全流程中，远程模型偶发返回未闭合 HTML，安全与完整性校验正确拒绝了该结果，没有启用假模板。

正式修复包括：

- 记录流式响应的 `finish_reason`。
- 将每次远程响应保存到对应运行目录，便于审计截断原因。
- 仅当错误全部属于 HTML 截断或结构不完整时，允许同一真实模型进行一次受控补全。
- 补全结果仍必须通过完整性和安全校验。
- 原始提示词要求控制 HTML/CSS 体量，优先保证内容、必需图片、响应式布局和闭合标签完整。

## Public 全流程验收

- 测试任务：`cmtgnmlsj00031js6ihx764av`
- 3 张风格图并行生成：约 76.7 秒
- 文案生成：约 25.6 秒
- 最终 HTML：约 142.5 秒
- 模型：`gpt-5.6-sol`
- 请求次数：1
- `finish_reason`：`stop`
- 流事件：6221
- 输入 tokens：15211
- 输出 tokens：6554
- 总 tokens：21765
- HTML 字符数：16857
- 卡住任务：0

验收产物：

- OSS 预览：`https://ai-site-generated-prod.oss-cn-guangzhou.aliyuncs.com/ai-website-workbench/generated/codex-sites/cmtgnmlsj00031js6ihx764av-1788145548672/index.html`
- 标准交付 ZIP：`https://ai-site-generated-prod.oss-cn-guangzhou.aliyuncs.com/ai-website-workbench/generated/delivery-packages/standard-delivery-cmtgnmlsj00031js6ihx764av-1788145700888.zip`
- Netlify：`https://xyste2e030401-xyst41.netlify.app`
- 本地报告：`D:\codex002\ai-image-mvp-stage1\logs\e2e-site-flow-20260831030401.json`

上述官网、OSS 预览和 Netlify 链接均返回 HTTP 200，TLS 校验正常。

## 运行状态与边界

- `xinyingst-ai-site`：online
- `xinyingst-site-worker`：online
- Worker ID：`ecs-remote-html-worker-01`
- `eaif-api-gateway`：保持 online，未重启
- `eaif-mobile-h5`：保持 online，未重启
- `SITE_GENERATOR_PROVIDER=remote_html`
- `SITE_GENERATION_MODE=worker_queue`
- `REMOTE_SITE_TIMEOUT_MS=900000`
- `ENABLE_MOCK_FALLBACK=false`
- `SITE_GENERATOR_ENABLE_TEMPLATE_FALLBACK=false`

## 已知非阻断提示

Netlify 部署状态为 `published`，公开 URL 已验证 200；部署后的可选 Supabase 元数据同步返回 `fetch failed`，因此该测试任务保留了 `publishError` 文本。它不影响客户访问、OSS 预览、交付 ZIP 或 Netlify 上线，但后续可将这类同步告警从“发布错误”字段中拆分，避免后台产生误导。

## 回滚

如需回滚，将 `/opt/xinyingst/current` 指回回滚记录中的 release，仅重建 `xinyingst-ai-site` 和 `xinyingst-site-worker`。不要改动 `eaif-api-gateway`、`eaif-mobile-h5`、Nginx、数据库或 OSS 配置。
